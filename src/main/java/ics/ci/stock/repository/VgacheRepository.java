package ics.ci.stock.repository;

import ics.ci.stock.entity.Vgache;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VgacheRepository extends JpaRepository<Vgache,Long> {
}
