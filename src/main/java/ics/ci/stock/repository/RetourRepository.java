package ics.ci.stock.repository;

import ics.ci.stock.entity.Retour;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RetourRepository extends JpaRepository<Retour, Long> {
}
